prometheus:
  chart: kube-prometheus-stack
  repository: prometheus-community
  version: "77.11.0"

grafana:
  chart: grafana
  repository: grafana
  version: "9.4.0"

loki:
  chart: loki
  repository: grafana
  version: "6.43.0"

tempo:
  chart: tempo
  repository: grafana
  version: "1.24.4"

# Kiali is installed by platform/access/, not by this module, and its version is
# declared in platform/access/versions.yaml — the file its installer reads. This
# entry was a third opinion about a component nothing installed.